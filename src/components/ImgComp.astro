---
import { Image } from 'astro:assets';
const { src, alt, loading = "lazy", class: className = "" } = Astro.props;
---

<Image src={src} alt={alt} loading={loading} width={80} height={200} class={className} />
